Inventory & Search App

A simple Angular application demonstrating component interaction, data sharing via Services, and form handling. This project utilizes Angular's Standalone API and a custom folder structure.

Project Structure

Note: This project uses a specific folder structure where HTML template files are stored in a subfolder named `html` within each component directory.
